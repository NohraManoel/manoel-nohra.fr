---
// src/layouts/BaseLayout.astro
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
const currentLang = Astro.currentLocale || 'en';

const defaultDesc = currentLang === 'en' 
  ? "Manoël Nohra - Software Engineer & Gameplay Programmer Portfolio. Explore game development projects and professional journey."
  : "Manoël Nohra - Portfolio de Génie Logiciel & Programmation Gameplay. Découvrez ses projets de jeu et son parcours professionnel.";

const metaDescription = description || defaultDesc;
const siteUrl = Astro.site || 'https://manoel-nohra.fr';
const canonicalURL = new URL(Astro.url.pathname, siteUrl);
const socialImageURL = new URL('/images/profile.jpg', siteUrl);

// On importe notre composant de navigation isolé
import Navigation from '../components/Navigation.astro';
---

<!DOCTYPE html>
<html lang={currentLang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | Portfolio</title>
    
    <!-- SEO Meta Tags -->
    <meta name="description" content={metaDescription} />
    <link rel="canonical" href={canonicalURL} />
    <meta name="generator" content={Astro.generator} />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url} />
    <meta property="og:title" content={`${title} | Portfolio`} />
    <meta property="og:description" content={metaDescription} />
    <meta property="og:image" content={socialImageURL} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={Astro.url} />
    <meta property="twitter:title" content={`${title} | Portfolio`} />
    <meta property="twitter:description" content={metaDescription} />
    <meta property="twitter:image" content={socialImageURL} />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;800&family=Outfit:wght@500;700;900&display=swap" rel="stylesheet">
  </head>
  <body>
    <Navigation />

    <main>
      <slot />
    </main>

    <footer>
      <p>&copy; {new Date().getFullYear()} - {currentLang === 'en' ? 'My Game Portfolio' : 'Mon Portfolio de Jeux'}</p>
    </footer>
  </body>
</html>

<style is:global>
  /* Styles globaux pour tout le site (couleurs, police, mise en page globale) */
  :root {
    --bg-dark: #070b14;
    --bg-card: rgba(20, 31, 54, 0.6);
    --bg-card-hover: rgba(30, 45, 75, 0.8);
    --accent: #3b82f6;
    --accent-light: #60a5fa;
    --accent-glow: rgba(59, 130, 246, 0.5);
    --text-main: #f8fafc;
    --text-muted: #94a3b8;
    --border-light: rgba(255, 255, 255, 0.08);
    --border-glow: rgba(96, 165, 250, 0.3);
  }

  html {
    scroll-behavior: smooth;
    scroll-padding-top: 80px; /* Accounts for the sticky navbar */
  }

  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    font-family: 'Inter', sans-serif;
    background-color: var(--bg-dark);
    color: var(--text-main);
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    overflow-x: hidden;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: 'Outfit', sans-serif;
  }

  main {
    flex: 1;
    max-width: 1000px;
    width: 100%;
    margin: 0 auto;
    padding: 2rem 1rem;
  }

  footer {
    text-align: center;
    padding: 2rem;
    color: var(--text-muted);
    font-size: 0.9rem;
    border-top: 1px solid var(--bg-card);
  }

  a {
    color: var(--accent);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  a:hover {
    color: var(--accent-light);
  }
</style>